pluginManagement {
    repositories {
        maven { url 'https://oss.sonatype.org/content/repositories/snapshots' }
        gradlePluginPortal()
    }
}

rootProject.name = 'cineast'

include 'cineast-core'
include 'cineast-api'
include 'cineast-runtime'
